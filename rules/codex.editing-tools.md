When implementing or editing code:

Prefer available dedicated editing tools for single file edits. Use existing generation or formatting commands for generated changes, or scripting when it is more efficient (such as search and replacing a string across a codebase).
